BEGIN CODE

    STRING a = "MY TEST", b = "ANOTHER TEST", c = "TEST"
    DISPLAY: a & b & c
    # x=y=4
    # DISPLAY: x & t & z & $ & a_1 & [#] & "last"
    # DISPLAY: "Enter value for x and y separated by comma like this example: '1,2' "
    # SCAN: x, y

    # IF (x == y)
    # BEGIN IF
    #     DISPLAY: "x is equal to y"
    # END IF
    # ELSE IF (x > y)
    # BEGIN IF
    #     DISPLAY: "x is greater than to y"
    # END IF
    # ELSE IF (x >= y)
    # BEGIN IF
    #     DISPLAY: "x is greater than or equal to y"
    # END IF
    # ELSE IF (x < y)
    # BEGIN IF
    #     DISPLAY: "x is less than to y"
    # END IF
    # ELSE IF (x <= y)
    # BEGIN IF
    #     DISPLAY: "x is not equal to y"
    # END IF
    # ELSE
    # BEGIN IF
    #     DISPLAY: "x is not equal to y"
    # END IF

END CODE